source "https://rubygems.org"

gem "jekyll", "~> 4.4.1"
gem "jekyll-minifier", "~> 0.0.1"

group :jekyll_plugins do
  # Keep only plugins compatible with Jekyll 4.x
  # gem "jekyll-livereload", "~> 0.2" # Removed due to Jekyll 3.x compatibility
end

# Use Bundler for gem management
# gem "bundler", "~> 2.4"
